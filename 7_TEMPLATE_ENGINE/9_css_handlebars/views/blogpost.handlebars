{{#with post}}
    <h1>{{title}}</h1>
    <h4>{{category}}</h4>
    <p>{{content}}</p>
    <br>
    <h4>Seção de comentários</h4>
    {{#each comments}}
        <p>{{this}}</p>
    {{/each}}
{{/with}}